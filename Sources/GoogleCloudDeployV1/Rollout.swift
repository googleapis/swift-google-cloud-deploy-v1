// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// A `Rollout` resource in the Cloud Deploy API.
///
/// A `Rollout` contains information around a specific deployment to a `Target`.
public struct Rollout: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Identifier. Name of the `Rollout`. Format is
  /// `projects/{project}/locations/{location}/deliveryPipelines/{deliveryPipeline}/releases/{release}/rollouts/{rollout}`.
  /// The `rollout` component must match `[a-z]([a-z0-9-]{0,61}[a-z0-9])?`
  public var name: Swift.String = Swift.String()

  /// Output only. Unique identifier of the `Rollout`.
  public var uid: Swift.String = Swift.String()

  /// Optional. Description of the `Rollout` for user purposes. Max length is 255
  /// characters.
  public var description: Swift.String = Swift.String()

  /// Optional. User annotations. These attributes can only be set and used by
  /// the user, and not by Cloud Deploy. See
  /// https://google.aip.dev/128#annotations for more details such as format and
  /// size limitations.
  public var annotations: [Swift.String: Swift.String] = [:]

  /// Labels are attributes that can be set and used by both the
  /// user and by Cloud Deploy. Labels must meet the following constraints:
  ///
  /// * Keys and values can contain only lowercase letters, numeric characters,
  /// underscores, and dashes.
  /// * All characters must use UTF-8 encoding, and international characters are
  /// allowed.
  /// * Keys must start with a lowercase letter or international character.
  /// * Each resource is limited to a maximum of 64 labels.
  ///
  /// Both keys and values are additionally constrained to be <= 128 bytes.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Output only. Time at which the `Rollout` was created.
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Time at which the `Rollout` was approved.
  public var approveTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Time at which the `Rollout` was enqueued.
  public var enqueueTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Time at which the `Rollout` started deploying.
  public var deployStartTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Time at which the `Rollout` finished deploying.
  public var deployEndTime: GoogleCloudWkt.Timestamp? = nil

  /// Required. The ID of Target to which this `Rollout` is deploying.
  public var targetId: Swift.String = Swift.String()

  /// Output only. Approval state of the `Rollout`.
  public var approvalState: Rollout.ApprovalState = Rollout.ApprovalState()

  /// Output only. Current state of the `Rollout`.
  public var state: Rollout.State = Rollout.State()

  /// Output only. Additional information about the rollout failure, if
  /// available.
  public var failureReason: Swift.String = Swift.String()

  /// Output only. The resource name of the Cloud Build `Build` object that is
  /// used to deploy the Rollout. Format is
  /// `projects/{project}/locations/{location}/builds/{build}`.
  public var deployingBuild: Swift.String = Swift.String()

  /// This checksum is computed by the server based on the value of other
  /// fields, and may be sent on update and delete requests to ensure the
  /// client has an up-to-date value before proceeding.
  public var etag: Swift.String = Swift.String()

  /// Output only. The reason this rollout failed. This will always be
  /// unspecified while the rollout is in progress.
  public var deployFailureCause: Rollout.FailureCause = Rollout.FailureCause()

  /// Output only. The phases that represent the workflows of this `Rollout`.
  public var phases: [Phase] = []

  /// Output only. Metadata contains information about the rollout.
  public var metadata: Metadata? = nil

  /// Output only. Name of the `ControllerRollout`. Format is
  /// `projects/{project}/locations/{location}/deliveryPipelines/{deliveryPipeline}/releases/{release}/rollouts/{rollout}`.
  public var controllerRollout: Swift.String = Swift.String()

  /// Output only. Name of the `Rollout` that is rolled back by this `Rollout`.
  /// Empty if this `Rollout` wasn't created as a rollback.
  public var rollbackOfRollout: Swift.String = Swift.String()

  /// Output only. Names of `Rollouts` that rolled back this `Rollout`.
  public var rolledBackByRollouts: [Swift.String] = []

  /// Output only. The AutomationRun actively repairing the rollout.
  public var activeRepairAutomationRun: Swift.String = Swift.String()

  /// Initialize a new instance of `Rollout`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Rollout().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Valid approval states of a `Rollout`.
  public enum ApprovalState: Codable, Equatable, Sendable {
    /// The `Rollout` has an unspecified approval state.
    case unspecified
    /// The `Rollout` requires approval.
    case needsApproval
    /// The `Rollout` does not require approval.
    case doesNotNeedApproval
    /// The `Rollout` has been approved.
    case approved
    /// The `Rollout` has been rejected.
    case rejected
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .needsApproval: return 1
      case .doesNotNeedApproval: return 2
      case .approved: return 3
      case .rejected: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "APPROVAL_STATE_UNSPECIFIED"
      case .needsApproval: return "NEEDS_APPROVAL"
      case .doesNotNeedApproval: return "DOES_NOT_NEED_APPROVAL"
      case .approved: return "APPROVED"
      case .rejected: return "REJECTED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ApprovalState/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "APPROVAL_STATE_UNSPECIFIED": self = .unspecified
      case "NEEDS_APPROVAL": self = .needsApproval
      case "DOES_NOT_NEED_APPROVAL": self = .doesNotNeedApproval
      case "APPROVED": self = .approved
      case "REJECTED": self = .rejected
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ApprovalState/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .needsApproval
      case 2: self = .doesNotNeedApproval
      case 3: self = .approved
      case 4: self = .rejected
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .needsApproval: return try container.encode(1)
      case .doesNotNeedApproval: return try container.encode(2)
      case .approved: return try container.encode(3)
      case .rejected: return try container.encode(4)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Valid states of a `Rollout`.
  public enum State: Codable, Equatable, Sendable {
    /// The `Rollout` has an unspecified state.
    case unspecified
    /// The `Rollout` has completed successfully.
    case succeeded
    /// The `Rollout` has failed.
    case failed
    /// The `Rollout` is being deployed.
    case inProgress
    /// The `Rollout` needs approval.
    case pendingApproval
    /// An approver rejected the `Rollout`.
    case approvalRejected
    /// The `Rollout` is waiting for an earlier Rollout(s) to complete on this
    /// `Target`.
    case pending
    /// The `Rollout` is waiting for the `Release` to be fully rendered.
    case pendingRelease
    /// The `Rollout` is in the process of being cancelled.
    case cancelling
    /// The `Rollout` has been cancelled.
    case cancelled
    /// The `Rollout` is halted.
    case halted
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .succeeded: return 1
      case .failed: return 2
      case .inProgress: return 3
      case .pendingApproval: return 4
      case .approvalRejected: return 5
      case .pending: return 6
      case .pendingRelease: return 7
      case .cancelling: return 8
      case .cancelled: return 9
      case .halted: return 10
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "STATE_UNSPECIFIED"
      case .succeeded: return "SUCCEEDED"
      case .failed: return "FAILED"
      case .inProgress: return "IN_PROGRESS"
      case .pendingApproval: return "PENDING_APPROVAL"
      case .approvalRejected: return "APPROVAL_REJECTED"
      case .pending: return "PENDING"
      case .pendingRelease: return "PENDING_RELEASE"
      case .cancelling: return "CANCELLING"
      case .cancelled: return "CANCELLED"
      case .halted: return "HALTED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "STATE_UNSPECIFIED": self = .unspecified
      case "SUCCEEDED": self = .succeeded
      case "FAILED": self = .failed
      case "IN_PROGRESS": self = .inProgress
      case "PENDING_APPROVAL": self = .pendingApproval
      case "APPROVAL_REJECTED": self = .approvalRejected
      case "PENDING": self = .pending
      case "PENDING_RELEASE": self = .pendingRelease
      case "CANCELLING": self = .cancelling
      case "CANCELLED": self = .cancelled
      case "HALTED": self = .halted
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .succeeded
      case 2: self = .failed
      case 3: self = .inProgress
      case 4: self = .pendingApproval
      case 5: self = .approvalRejected
      case 6: self = .pending
      case 7: self = .pendingRelease
      case 8: self = .cancelling
      case 9: self = .cancelled
      case 10: self = .halted
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .succeeded: return try container.encode(1)
      case .failed: return try container.encode(2)
      case .inProgress: return try container.encode(3)
      case .pendingApproval: return try container.encode(4)
      case .approvalRejected: return try container.encode(5)
      case .pending: return try container.encode(6)
      case .pendingRelease: return try container.encode(7)
      case .cancelling: return try container.encode(8)
      case .cancelled: return try container.encode(9)
      case .halted: return try container.encode(10)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Well-known rollout failures.
  public enum FailureCause: Codable, Equatable, Sendable {
    /// No reason for failure is specified.
    case unspecified
    /// Cloud Build is not available, either because it is not enabled or because
    /// Cloud Deploy has insufficient permissions. See [required
    /// permission](https://cloud.google.com/deploy/docs/cloud-deploy-service-account#required_permissions).
    case cloudBuildUnavailable
    /// The deploy operation did not complete successfully; check Cloud Build
    /// logs.
    case executionFailed
    /// Deployment did not complete within the allotted time.
    case deadlineExceeded
    /// Release is in a failed state.
    case releaseFailed
    /// Release is abandoned.
    case releaseAbandoned
    /// No Skaffold verify configuration was found.
    case verificationConfigNotFound
    /// Cloud Build failed to fulfill Cloud Deploy's request. See failure_message
    /// for additional details.
    case cloudBuildRequestFailed
    /// A Rollout operation had a feature configured that is not supported.
    case operationFeatureNotSupported
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .cloudBuildUnavailable: return 1
      case .executionFailed: return 2
      case .deadlineExceeded: return 3
      case .releaseFailed: return 4
      case .releaseAbandoned: return 5
      case .verificationConfigNotFound: return 6
      case .cloudBuildRequestFailed: return 7
      case .operationFeatureNotSupported: return 8
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "FAILURE_CAUSE_UNSPECIFIED"
      case .cloudBuildUnavailable: return "CLOUD_BUILD_UNAVAILABLE"
      case .executionFailed: return "EXECUTION_FAILED"
      case .deadlineExceeded: return "DEADLINE_EXCEEDED"
      case .releaseFailed: return "RELEASE_FAILED"
      case .releaseAbandoned: return "RELEASE_ABANDONED"
      case .verificationConfigNotFound: return "VERIFICATION_CONFIG_NOT_FOUND"
      case .cloudBuildRequestFailed: return "CLOUD_BUILD_REQUEST_FAILED"
      case .operationFeatureNotSupported: return "OPERATION_FEATURE_NOT_SUPPORTED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:FailureCause/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "FAILURE_CAUSE_UNSPECIFIED": self = .unspecified
      case "CLOUD_BUILD_UNAVAILABLE": self = .cloudBuildUnavailable
      case "EXECUTION_FAILED": self = .executionFailed
      case "DEADLINE_EXCEEDED": self = .deadlineExceeded
      case "RELEASE_FAILED": self = .releaseFailed
      case "RELEASE_ABANDONED": self = .releaseAbandoned
      case "VERIFICATION_CONFIG_NOT_FOUND": self = .verificationConfigNotFound
      case "CLOUD_BUILD_REQUEST_FAILED": self = .cloudBuildRequestFailed
      case "OPERATION_FEATURE_NOT_SUPPORTED": self = .operationFeatureNotSupported
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:FailureCause/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .cloudBuildUnavailable
      case 2: self = .executionFailed
      case 3: self = .deadlineExceeded
      case 4: self = .releaseFailed
      case 5: self = .releaseAbandoned
      case 6: self = .verificationConfigNotFound
      case 7: self = .cloudBuildRequestFailed
      case 8: self = .operationFeatureNotSupported
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .cloudBuildUnavailable: return try container.encode(1)
      case .executionFailed: return try container.encode(2)
      case .deadlineExceeded: return try container.encode(3)
      case .releaseFailed: return try container.encode(4)
      case .releaseAbandoned: return try container.encode(5)
      case .verificationConfigNotFound: return try container.encode(6)
      case .cloudBuildRequestFailed: return try container.encode(7)
      case .operationFeatureNotSupported: return try container.encode(8)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.deploy.v1.Rollout"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
